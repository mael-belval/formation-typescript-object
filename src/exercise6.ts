/**
 * Exercise 6 - Polymorphism
 * 
 * We now want to create a class to represent a calendar. A calendar contains a list of events of different types.
 * 
 * 1. Create a class Calendar. It has a property events that is an set of SingleDayEvent.
 * 
 * 2. Create a method addEvent(event) that adds an event to the calendar.
 * 
 * 3. What is the association between the classes Calendar and the events classes ? Complete the UML diagram.
 * 
 * 4. Create a method print() that prints all the events in the calendar.
 */